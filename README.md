# dotfiles
Personal Arch Linux dotfiles and configuration.

To use this configuration, launch the `install.sh` script.

First, you need to make the script executable:

```bash
chmod +x install.sh
```

After that Launch the script:
```bash
./install.sh
```
